/*
For debug purpuses
let ws = new WebSocket("ws://localhost:8085");

export default {
    log(message: string) {
        const tmp = Date.now() + ": " + message;
        console.log(tmp);
        ws.send(tmp);
    }
} */